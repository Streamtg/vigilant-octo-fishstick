name: Simulate Real Visitor with Proxies and Device Emulation

on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

jobs:
  simulate-visitor:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Install system dependencies for Playwright
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-0 libxss1 libasound2 libnss3 libxshmfence1 \
            libgbm1 libxcomposite1 libxrandr2 libxdamage1 libxtst6 libx11-xcb1 \
            libdrm2 libxinerama1 libatk-bridge2.0-0 libcups2 libx11-dev \
            libegl1 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libglu1-mesa \
            gstreamer1.0-plugins-base gstreamer1.0-plugins-good gstreamer1.0-libav \
            gstreamer1.0-plugins-ugly gstreamer1.0-tools gstreamer1.0-x gstreamer1.0-gl \
            gstreamer1.0-alsa gstreamer1.0-pulseaudio gstreamer1.0-plugins-bad \
            libopus0 libvpx7 libwoff1 libevent-2.1-7 libflite1 libsecret-1-0 \
            libharfbuzz-icu0 libavif13 libx264-163

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Playwright and Axios
        run: |
          npm init -y
          npm install playwright axios
          npx playwright install

      - name: Create simulate.js
        run: |
          cat <<'EOF' > simulate.js
          const { chromium, firefox, webkit } = require('playwright');
          const axios = require('axios');

          const urls = [
            'https://yoelmod.blogspot.com/2022/05/netflix-premium-free-mod-apk.html'
          ];

          const referers = [
            'https://t.co', 'https://facebook.com', 'https://twitter.com',
            'https://instagram.com', 'https://linkedin.com', 'https://reddit.com'
          ];

          const userAgents = {
            chromium: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/121.0.0.0 Safari/537.36',
            firefox: 'Mozilla/5.0 (Windows NT 10.0; rv:115.0) Gecko/20100101 Firefox/115.0',
            webkit: 'Mozilla/5.0 (iPhone; CPU iPhone OS 13_5 like Mac OS X) AppleWebKit/605.1.15 Version/13.1 Mobile/15E148 Safari/604.1'
          };

          (async () => {
            let proxyList = [];
            try {
              const res = await axios.get('https://api.proxyscrape.com/v2/?request=getproxies&protocol=http&timeout=5000&country=all&ssl=all&anonymity=elite');
              proxyList = res.data.split('\n').filter(p => p.includes(':'));
            } catch (e) {
              return;
            }

            const proxy = proxyList[Math.floor(Math.random() * proxyList.length)].trim();
            const referer = referers[Math.floor(Math.random() * referers.length)];
            const targetURL = urls[Math.floor(Math.random() * urls.length)];
            const browserName = ['chromium', 'firefox', 'webkit'][Math.floor(Math.random() * 3)];
            const browserEngine = { chromium, firefox, webkit }[browserName];

            const browser = await browserEngine.launch({
              headless: true,
              proxy: { server: 'http://' + proxy }
            });

            const context = await browser.newContext({
              viewport: { width: 1366, height: 768 },
              userAgent: userAgents[browserName],
              extraHTTPHeaders: {
                'referer': referer
              }
            });

            const page = await context.newPage();
            try {
              await page.goto(targetURL, { waitUntil: 'domcontentloaded', timeout: 20000 });

              for (let i = 0; i < 5; i++) {
                await page.mouse.move(Math.random() * 1300, Math.random() * 700);
                await page.mouse.wheel(0, 500);
                await page.waitForTimeout(Math.floor(Math.random() * 3000) + 1000);
              }

              const links = await page.$$eval('a', anchors =>
                anchors.filter(a => a.href.includes('yoelmod.blogspot.com') && a.offsetParent !== null).map(a => a.href)
              );

              if (links.length > 0) {
                const randomLink = links[Math.floor(Math.random() * links.length)];
                await page.goto(randomLink, { waitUntil: 'domcontentloaded' });
                await page.waitForTimeout(Math.floor(Math.random() * 7000) + 3000);
              }
            } catch (e) {}
            await browser.close();
          })();
          EOF

      - name: Run simulate.js
        run: node simulate.js
